var a;

// Anything put in an if as the statement will try to convert to a Boolean

a = "Hello"
// If 'a' is nothing then it will display false and not show anything to the console.
if (a) {
  console.log('Found something')
}
